/** GENERATED FROM BACKEND JAVA DTO. Do not add UI-only fields here. */
export interface WeeklyWorkoutVolumeDto {
  weekStart: string | null;
  weekEnd: string | null;
  currentWeekVolume: number | null;
  previousWeekVolume: number | null;
  percentageChange: number | null;
}
