/** GENERATED FROM BACKEND JAVA DTO. Do not add UI-only fields here. */
export interface WorkoutResponse {
  status: string | null;
  message: string | null;
  done: boolean | null;
  id: number | null;
}
